```sh
uv run python -m classifier.batch_process --cache .\cache\lastframe_256 --output-root .\output\final\diff_ths\th80 --period-threshold 35.0 --sign-threshold 0.8 --ignore-last-frames 3 --direct
```

--cache cache路径

--output-root

--period-threshold 周期阈值

--sign-threshold 符号率

--ignore-last-frames 3 忽略最后3frame 除去自注意力影响

--direct 只生成最终的csv







---

```
uv run python -m ablation.aggregate_labels --csv_dir .\output\sign\th40\ --output .\output\csv\final\diff_thp\                                                      
```

